public class MJC : GraphJumpEnv
{
    //the config is attached to the env, so each env can has it's own settings
    //the type is attached to the player, because an env can have multiple player types
}